# Default values for chart.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: openworklabs/lotus:v0.5.4-custom
  pullPolicy: Always

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

serviceAccount:
  # Specifies whether a service account should be created
  create: true
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: acc

podSecurityContext:
  fsGroup: 2000

securityContext:
  # capabilities:
  #   drop:
  #   - ALL
  readOnlyRootFilesystem: true
  runAsNonRoot: false
  runAsUser: 1000

# Using this varliable you can redefine Lotus service ports
service:
  type: ClusterIP
  port: 1234
  targetPort: 1234

ingress:
  enabled: true
  host: node.glif.io
  annotations:
    nginx.ingress.kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/enable-cors: "true"
    nginx.ingress.kubernetes.io/cors-allow-headers: "Authorization,Accept,Origin,DNT,X-CustomHeader,Keep-Alive,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Content-Length,Content-Range,Range"
    nginx.ingress.kubernetes.io/enable-rewrite-log: "true"
    nginx.ingress.kubernetes.io/enable-modsecurity: "true"
    nginx.ingress.kubernetes.io/modsecurity-snippet: |
      SecRuleEngine On
      SecDebugLog /tmp/modsec_debug.log
    # nginx.ingress.kubernetes.io/server-alias: "_"
  lotus:
    enabled: true
#    annotations: 
#      nginx.ingress.kubernetes.io/configuration-snippet: |
#        proxy_set_header Authorization "Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJBbGxvdyI6WyJyZWFkIiwid3JpdGUiLCJzaWduIiwiYWRtaW4iXX0.O58UONXPKPhtadDxr--MM3P6r8QeQoDySBJXAtlXeJU";
#      nginx.ingress.kubernetes.io/whitelist-source-range: "91.149.128.236/32"
  ipfs:
    enabled: false
  powergate:
    enabled: false

init:
  sync:
    enabled: true
  import:
    enabled: false
    # ideally a shared volume containing chain dump
    volume: {}
      #- name: filecoin-chain-vol
      #  persistentVolumeClaim:
      #    claimName: efs-claim

secretVolume:
  enabled: false

resources: 
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  lotus:
    requests:
      cpu: 1000m
      memory: 3Gi
    limits:
      cpu: 1800m
      memory: 6Gi
  ipfs:
    requests:
      cpu: 500m
      memory: 1Gi
    limits:
      cpu: 1000m
      memory: 2Gi
  powergate:
    requests:
      cpu: 500m
      memory: 1Gi
    limits:
      cpu: 1000m
      memory: 2Gi

persistence:
  enabled: true
  snapshots:
    uploadToIpfs: false
    enabled: true 
    automation:
      creation:
        enabled: true
      deletion: 
        enabled: true
        retention:
          count: 2
    restore:
      enabled: false
      name: snapshottest 
  ## Filecoin data Persistent Volume Storage Class
  ## If defined, storageClassName: <storageClass>
  ## If set to "-", storageClassName: "", which disables dynamic provisioning
  ## If undefined (the default) or set to null, no storageClassName spec is
  ##   set, choosing the default provisioner.  (gp2 on AWS, standard on
  ##   GKE, AWS & OpenStack)
  ##
  # storageClass: "-"
  lotus:
    size: "100Gi"
    accessModes:
      - ReadWriteOnce
  ipfs:
    size: "100Gi"
    accessModes:
      - ReadWriteOnce
  powergate:
    size: "100Gi"
    accessModes:
      - ReadWriteOnce

Powergate:
  enabled: false

IPFS:
  enabled: false
  
nodeSelector:
  enabled: true
  nodeLabel:
    role: worker

tolerations: []

affinity: {}
