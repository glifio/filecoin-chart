#we have a cache service for space06
cache:
  enabled: true
  replicas: 2
  resources:
    limits:
      cpu: 5
      memory: 12Gi
    requests:
      cpu: 500m
      memory: 4Gi

# init:
#   importSnapshot:
#     SNAPSHOTURL: "https://fil-chain-snapshots-fallback.s3.amazonaws.com/mainnet/minimal_finality_stateroots_latest.car"
#     enabled: true
#   clearRestart:
#     enabled: true

Lotus:
  service:
    gateway: true

nameOverride: ""
nodeSelector:
  nodeLabel:
    aws.amazon.com/eks-local-ssd: "true"
    lifecycle: OnDemand
    assignedPod: space06

persistence:
  enabled: true
  autoResizer:
    enabled: false
  lotus:
    accessModes:
    - ReadWriteOnce
    size: 3482Gi
    storageClassName: local-storage
  snapshots:
    automation:
      creation:
        enabled: false
      deletion:
        enabled: false
    enabled: false
    restore:
      enabled: false

resources:
  lotus:
    limits:
      cpu: 15
      memory: 120Gi
    requests:
      cpu: 12
      memory: 64Gi

secretVolume:
  enabled: true
