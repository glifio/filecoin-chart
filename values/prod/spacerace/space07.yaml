cache:
  enabled: true
  nodeSelector:
    nodeLabel:
      role: worker
  replicas: 2
  resources:
    limits:
      cpu: 5
      memory: 12Gi
    requests:
      cpu: 500m
      memory: 4Gi

ingress:
  lotus:
    enabled: true

nodeSelector:
  nodeLabel:
    lifecycle: OnDemand-r5-8x

persistence:
  snapshots:
    automation:
      creation:
        cron: "0 20 * * *"
      deletion:
        cron: "0 21 * * *"
    restore:
      enabled: true
      name: space07-t2b8f
  lotus:
    size: "32768Gi"
    accessModes:
      - ReadWriteOnce
    storageClassName: "ebs-sc-io2"
    lotusVolumeName: "vol-lotus-io2"

resources:
  lotus:
    requests:
      cpu: 23
      memory: 160Gi
    limits:
      cpu: 29
      memory: 240Gi
