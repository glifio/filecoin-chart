1. Get the application URL by running these commands:
{{- if .Values.ingress.enabled }}
  http://{{ .Values.ingress.host }}
IPFS, Powergate and Lotus node will be available by /ipfs, /powergate, and /lotus paths accordingly if enabled.
{{- end }}
