{{- if .Values.createCanaryService }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-canary-lotus-service
  labels:
    app: lotus-node-canary
  annotations:
  {{- range $key, $value := .Values.canaryServiceAnnotations }}
      {{ $key }}: {{ $value | quote }}
  {{- end }}
spec:
  type: ClusterIP
  selector:
  {{- range $key, $value := .Values.canaryServiceSelectors }}
      {{ $key }}: {{ $value | quote }}
  {{- end }}
  ports:
    - protocol: TCP
      port: 1234
      targetPort: 1234
      name: tcp-api
    {{- if .Values.lotusEnv.INFRA_LOTUS_GATEWAY }}
    - protocol: TCP
      port: 2346
      targetPort: 2346
      name: gateway
    {{- end}}
{{- end }}
